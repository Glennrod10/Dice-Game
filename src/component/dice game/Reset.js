
const Reset = ({reset}) => {
  return (
    <div className="text-center mt-4">
        <button onClick={reset} className="btn btn-outline-dark">Reset Game</button>
    </div>
  )
}

export default Reset
